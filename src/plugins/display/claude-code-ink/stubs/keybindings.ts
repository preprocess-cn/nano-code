export function useKeybinding(_key: string, _handler: () => void): void {
  // no-op in nano-code
}

export function useKeybindings(_keybindings: Array<{ key: string; handler: () => void }>): void {
  // no-op in nano-code
}
